export interface Theme {
  color: {
    accent: string;
    light: string;
    medium: string;
    dark: string;
  };
}

export const theme: Theme = {
  color: {
    accent: '#ffff4c',
    light: '#dddddd',
    medium: '#cccccc',
    dark: '#333333',
  },
};
